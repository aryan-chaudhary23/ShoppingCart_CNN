services:
  - type: web
    name: smartcart-backend
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    runtime: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
    rootDir: backend
